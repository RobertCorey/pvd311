rules_version = '2';

// Sunset lockdown: deny all client access. Admin SDK bypasses rules.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
