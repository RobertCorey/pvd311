# legacy/

`automation/` — the PVD Snow laptop-era Node server (Playwright + dashboard). Superseded 2026-08-22 by `worker/` (Cloudflare Workers + Browser Run), which has full parity: engine, HITL, watcher, canary, digest, app API. Kept for reference and as the VPS/Docker fallback (see `legacy/automation/DEPLOY.md`). Not built by CI.
